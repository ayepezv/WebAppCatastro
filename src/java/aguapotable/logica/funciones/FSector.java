package aguapotable.logica.funciones;

import accesoDatos.AccesoDatos;
import catastro.logica.entidades.Sector;
import java.sql.ResultSet;
import java.util.ArrayList;
import java.util.List;

public class FSector {
/*
    public static List<Sector> obtenerSectores() throws Exception {
        List<Sector> lst = new ArrayList<>();
        AccesoDatos accesoDatos;
        Sector sector;
        ResultSet resultSet;
        String sql;
        try {
            accesoDatos = new AccesoDatos();
            sql = "SELECT * from agua_potable.f_seleccionar_sectores();";
            resultSet = accesoDatos.ejecutaQuery(sql);
            while (resultSet.next()) {
                sector = new Sector();
                sector.setIdSector(resultSet.getInt("sr_id_sector"));
                /*int_id_ciclo 
                sector.setNombre(resultSet.getString("chv_nombre"));
                sector.set
    
    
    private int int_secuencia_sector;
    private String ch_estado_logico;
    private Timestamp ts_fecha_logico;
    private Timestamp ts_fecha_registro;
    private Timestamp ts_fecha_actualizacion;
    private Timestamp ts_fecha_baja;
    private Usuario session_usuario;
            }
        } catch (Exception e) {
            throw e;
        }
        return lst;
    }
    */
    
}
